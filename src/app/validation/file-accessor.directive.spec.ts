import { FileAccessorDirective } from './file-accessor.directive';

describe('FileAccessorDirective', () => {
  it('should create an instance', () => {
    const directive = new FileAccessorDirective();
    expect(directive).toBeTruthy();
  });
});
